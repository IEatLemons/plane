/**
 * Compatibility stub: live resolves `@plane/utils` via package.json workspace deps,
 * not this file. Kept so path/tsconfig expectations do not reference an empty module.
 */
export const __planeLiveUtilsStub = 0;
