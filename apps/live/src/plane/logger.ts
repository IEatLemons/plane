/**
 * Compatibility stub: live resolves `@plane/logger` via package.json workspace deps,
 * not this file. Kept so path/tsconfig expectations do not reference an empty module.
 */
export const __planeLiveLoggerStub = 0;
